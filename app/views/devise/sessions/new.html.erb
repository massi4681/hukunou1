<div class="container">
  <div class="row">
    <div class="col-xs-12">
<h4>会員の方はこちらからログイン</h4>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :メールアドレス %>
    <span class="sign-in-email"><%= f.email_field :email, autofocus: true, autocomplete: "email" %></span>
  </div>

  <div class="field">
    <%= f.label :パスワード %>
    <span class="sign-in-password"><%= f.password_field :password, autocomplete: "current-password" %></span>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "ログイン", class:"btn btn-info log" %>
  </div>
<% end %>

<h4>登録がお済みでない方</h4>
<%= link_to 'こちら', new_user_registration_path %>から新規登録を行なってください。
</div>
</div>
</div>
